# ─── Bal Vakti 🐝 ortam değişkenleri ───────────────────────────
# Bu dosyayı .env olarak kopyala ve değerleri doldur.

# Telegram BotFather'dan aldığın token (https://t.me/BotFather → /newbot)
BOT_TOKEN=

# Botun kullanıcı adı (t.me/ adresindeki kısım, @ olmadan) — davet linkleri için
BOT_USERNAME=

# Vercel'e deploy ettikten sonraki adresin (https://proje-adin.vercel.app)
# Yerel testte: boş bırak ya da http://localhost:8787 yaz
APP_URL=

# Webhook güvenlik anahtarı (kendin uydur, uzun bir metin)
WEBHOOK_SECRET=

# Demo modu: initData olmadan da /api testleri çalışsın (1 = açık)
# Prodüksiyonda 0 yap!
ALLOW_DEMO=0

# ─── 👑 Tanrı Modu (Admin Paneli) ────────────────────────────
# İki yöntemden EN AZ BİRİ gerekli:
# 1) OWNER_ID — senin Telegram ID'n (botta /admin yazınca bot söyler)
#    Bunu verirsen botta /admin → "Admin Panel" butonu çıkar (initData ile giriş).
# 2) ADMIN_PASSWORD — panel şifresi (https://<app>/admin.html → şifre girişi)
OWNER_ID=
ADMIN_PASSWORD=

# ─── Veritabanı ─────────────────────────────────────────────
# ✅ ÖNERİLEN: Firebase Realtime Database (ücretsiz, hızlı, tek istekte tüm veri)
# Firebase Console → Realtime Database → Create → URL'yi yapıştır:
#   https://proje-adin-default-rtdb.europe-west1.firebasedatabase.app
# ⚠️ Kurallar açık olmalı: Rules → ".read": true, ".write": true (demo için)
FIREBASE_DB_URL=

# (Eski seçenek — yedek) Upstash Redis: https://console.upstash.com → Create Database
# FIREBASE_DB_URL boşsa bunlar kullanılır
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
